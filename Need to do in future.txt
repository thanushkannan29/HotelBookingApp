1. PaymentStatus.Failed — Is It Used
You're right — Failed is never set anywhere. In TransactionService.CreatePaymentAsync, the transaction is created directly with PaymentStatus.Success (simulating an always-success payment gateway). Failed exists in the enum but is dead code.
Recommendation Keep the enum value — it's good design for when you integrate a real payment gateway. No code change needed now.

2. Direct Refund Endpoint — Analysis & Fix
Your understanding is correct. Here's the clean design

Guest Can directly refund within 30 minutes of payment (UI enforces the time window by checking TransactionDate). After 30 min, UI hides the button — but the backend should also enforce it.
Admin The legacy direct refund (POST apitransactions{id}refund) should be removed from Admin — Admin already has the proper approvereject flow via apiadminrefund-requests.

Changes needed in TransactionService.RefundAsync

Add a 30-minute window check for Guest role
Pass userId + role so the service can distinguish Guest vs Admin

Updated files needed SharedControllers.cs and TransactionService.cs

3. Background Services — Are Any Missing
Your 3 existing services cover the core flows well. After checking against the spec
NeedCoveredExpire unpaid reservations✅ ReservationCleanupServiceHotel deactivation auto-refund✅ HotelDeactivationRefundServiceNo-show marking✅ NoShowAutoCancelServiceEmailSMS confirmation (Spec #5)❌ Not implemented — acceptable, needs external SMTPSMS provider
No new background service needed. Email notifications are optional infrastructure.

4. Spec Coverage Check
SpecBackend Status1. Auth  JWT✅ Full2. Hotel search by citydates✅ Full3. Room selection & multi-room booking✅ Full4. Pricing  payment methods✅ Full (simulated gateway)5. Booking confirmation⚠️ Reservation code returned — emailSMS needs external service6. User profiles✅ Full7. Booking history✅ Full8. Hotel info & reviews✅ Full9. Cancellations & refunds✅ Full10. Additional services❌ Not in scope (optional)11. Responsive designNA (frontend concern)12. Security (JWT + rate limit + hashing)✅ Full13. SQL Server✅ Full14. API Integration✅ (self-contained, no external hotel API needed)15–16. TestingDeploymentNA

Files That Need Changes
Only 2 files need updates
1. TransactionService.cs — Add 30-min guest window check, remove Admin from direct refund logic
2. SharedControllers.cs — Restrict direct refund to Guest only, pass userId+role
Here are the complete updated files